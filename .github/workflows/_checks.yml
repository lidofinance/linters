name: Static Checks

on: push

jobs:
  security:
    uses: lidofinance/linters/github/workflows/security.yml
    with:
      skip-codeql: true

  actions:
    uses: lidofinance/linters/github/workflows/actions.yml
